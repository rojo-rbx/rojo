--[[
	Defines the process of "hydration" -- matching up a virtual DOM with
	concrete instances and assigning them IDs.
]]

local function hydrate(instanceMap, virtualInstanceMap, rootId, rootInstance)
	error("unimplemented")
end

return hydrate