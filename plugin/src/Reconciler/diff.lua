--[[
	Defines the process for diffing a virtual DOM and the real DOM to compute a
	patch that can be later applied.
]]

local function diff(instanceMap, virtualInstanceMap, rootId, rootInstance)
	error("unimplemented")
end

return diff